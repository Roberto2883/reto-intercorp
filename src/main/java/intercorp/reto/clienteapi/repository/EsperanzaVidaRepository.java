package intercorp.reto.clienteapi.repository;

import intercorp.reto.clienteapi.model.EsperanzaVida;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EsperanzaVidaRepository extends JpaRepository<EsperanzaVida, Integer> {
}
